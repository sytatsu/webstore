<a  href="{{ $href }}" target="_blank" class="block duration-300 hover:scale-110"
    style="min-width: {{ $dimensions }}px; min-height: {{ $dimensions }}px">
    <img class="p-3" src="{{ $src }}" alt="{{ $alt }}" width="{{ $dimensions }}px"/>
</a>
